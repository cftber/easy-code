Circuit Breaker 同步调用异步调用区别：
异步调用可中断，执行超时场景可直接失败，同步无法失败，只能汇报超时状态

CommandMaxConcurrentCount:
可以分配给一种远程调用的最大并发线程数，如果一种远程调用消耗的线程数超过此阀值，Hystrix会自动隔离限流，
拒绝掉超出处理能力的请求，以保护应用程序处理能力，防止整个应用程序被拖死。一般应用程序最大能使用的线程数是CPU核心数 * 100，
比如4核机器总的线程数是400。如果这个设置的值过大，Hystrix隔离限流功能会失效，应用程序可能被一个远程调用故障拖死。

